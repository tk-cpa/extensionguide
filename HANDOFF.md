# Extension Guide - Session Handoff

Last updated: 2026-09-23, end of T9 session (adds excise-tax-extension.html, sales-and-use-tax-extension.html, other-extensions.html; T9 continued three times mid-session to expand the sales/use tax survey - 9 to 21 to 27 to 46 jurisdictions - reaching full coverage of every state that imposes a general sales tax, plus DC). Jurisdictional/tax-type coverage: all 50 states + DC + Puerto Rico + 8 city/local jurisdictions + state estate/inheritance tax + federal excise + federal payroll tax + FBAR + a complete 46-jurisdiction sales/use tax survey live. 81 HTML files, 111 extensions-data.json records. T9 also pinned exact statutory subsections for 3 of the 5 estate-tax states previously cited only to chapter/regulation level: Massachusetts (Mass. Gen. Laws c. 65C sect 10 plus TIR 16-10), New Jersey (N.J. Admin. Code sect 18:26-9.1, confirmed 4-month extension extendable to 6), and Pennsylvania (72 P.S. sect 9136(d), confirmed 6-month discretionary extension). Illinois and Rhode Island remain cited to chapter level after a genuine attempt to pin the exact subsection this session - no pinpoint citation was located after multiple targeted searches, so this is a named open item, not a gap glossed over. other-extensions.html now includes a SUI/SUTA survey covering all 50 states plus DC (49 resolved), built across four continuations from the original 8-state version. Five confirmed exceptions: Arkansas, Idaho, Missouri, New Hampshire, and Washington (details above), all now extensions-data.json records. Three narrow exceptions that do not amount to a general right: Connecticut, Tennessee, and South Carolina (payment-only extensions or reimbursing-employers-only). The rest of the states with a definitive answer show no general filing-extension mechanism (North Carolina has granted disaster-specific relief before; Michigan granted only a 2026 one-time technical waiver). Only New Mexico remains unresolved, disclosed as a source-access failure (4 attempts, all rejected at the server level) rather than assumed either way - this is the honest ceiling of the survey this session. faq.html was revisited and now has entries for estate/inheritance, excise, sales/use tax, and payroll/SUI/FBAR. Open items, disclosed rather than rounded up: Kentucky and Nebraska's inheritance tax (a separate tax type from Nebraska's SUI/SUTA finding above, which resolved as "no provision located") have no general inheritance-tax filing-extension mechanism identified; Illinois and Rhode Island estate tax extension authority is cited to chapter/regulation level, pinpoint subsection not located; New Mexico's SUI/SUTA status is the only remaining gap in that survey.

## What this is

Site name: **Extension Guide** (a tk.cpa resource). Domain: **extensionguide.com** (purchased, DNS pointed at GitHub Pages). Repo: **tk-cpa/extensionguide** on GitHub, public. Deployment: **GitHub Pages**, branch `main`, root, static HTML (no build step). This is a brand-new, standalone site - not a rebrand of cpavalidated.com or globaltaxguide.com, though it shares their design system and workflow conventions.

Core locked slogan, must appear on every page: **"An extension to file is not an extension to pay tax."**

Scope: comprehensive public reference on US federal, state, and local tax filing extensions - all entity/tax types (individuals, C corps, S corps, partnerships, estates/trusts, nonprofits, information returns, estate tax, excise). Direct links to actual extension FORM PDFs on official tax-authority sites (not third-party mirrors) so users can print them, plus statute/authority citations for every rule.

## Current build status

**T1 - Federal (live):** index.html, disclaimer.html, faq.html, master-extension-matrix.html, federal-hub.html, and 5 federal form pages (4868, 7004, 8868, 4768, 8809), federal-special-situations.html, how-to-paper-file-extension.html, efile-and-third-party-services.html, extension-penalties-and-interest.html.

**T2/T3/T4 - All 50 states + DC (live):** every `<state>-extension.html` page exists and is cross-linked from states-hub.html, map.html, and the master matrix. Two system-level insights worth remembering: Ohio's ~600 municipalities and Michigan's ~24 taxing cities are each governed by a single statewide statute, which is why the local batch below consolidates them into one page apiece rather than hundreds of per-city pages.

**T5 - 8 city/local jurisdictions (live, this session):**
- `new-york-city-extension.html` - NYC individual tax rides on NY State Form IT-370 (no separate NYC form); business GCT/UBT uses Form NYC-6, automatic 6 months (up to two further 3-month extensions). N.Y.C. Admin. Code Sec. 11-605(1); NYC Rules tit. 19, ch. 11.
- `philadelphia-extension.html` - BIRT/NPT automatic, tied to the federal extension, no separate city form. Wage Tax has no individual extension mechanism (employer-withheld). Philadelphia Dept. of Revenue guidance.
- `ohio-municipal-extension.html` - covers all ~600 Ohio municipalities (RITA-administered, CCA-administered, or self-administered) under one statute, Ohio Rev. Code Sec. 718.05(G)(2).
- `michigan-cities-extension.html` - covers all ~24 Michigan cities with a city income tax, including Detroit, under one statute, MCL 141.664(1),(2). Written request required, up to 6 months, 70% payment threshold.
- `kansas-city-extension.html` - Kansas City, MO earnings tax, Form RD-112 (wage) / RD-111 (profits), automatic with timely filing and payment. KCMO Code of Ordinances ch. 68, art. VI.
- `st-louis-extension.html` - St. Louis, MO earnings tax, Form E-8 (individual) / Form E-234 (business), payment due with request. City of St. Louis Collector of Revenue guidance.
- `portland-multnomah-extension.html` - Portland/Multnomah County, OR combined business tax, Form EXT, 6 months, 90%/100% payment safe harbor. City of Portland/Multnomah County Revenue Division guidance.
- `san-francisco-extension.html` - SF Annual Business Tax Return extension request, not tied to the federal extension, full payment due by the original date. SF Office of the Treasurer and Tax Collector guidance.

**T6 - Puerto Rico (live, this session):**
- `puerto-rico-extension.html` - individuals and business entities both use the same form, Modelo SC 2644 ("Solicitud de Prorroga para Rendir la Planilla de Contribucion sobre Ingresos"), filed through SURI, automatic upon a timely request, typically 6 months, payment not extended. Authority: 13 L.P.R.A. Sec. 1061.03(c), 1061.06(c), 1061.07(c), 1061.09(b)(4), 1061.10(b)(2), 1061.16 (P.R. Internal Revenue Code), re-confirmed annually by a Carta Circular de Rentas Internas (CC RI 26-01 for tax year 2025). Note on the page itself: this covers the Puerto Rico Treasury (Hacienda) extension only, not federal filing questions for PR residents (IRC Sec. 933 and related), which are out of scope.

`extensions-data.json` now has 75 records (Federal + 50 states/DC + 8 local jurisdictions + Puerto Rico, with New York City, Philadelphia, and Puerto Rico each contributing 2 records for split regimes/filer types). `map.html`'s STATES object is fully populated - every FIPS code including "72" (Puerto Rico) now has a real `slug`, so there are no `slug:null` entries left. Its CITIES array carries a `slug` per city and all pins (state and city) render live/clickable; the map's "coming soon" legend swatch and related stale copy were removed since nothing is pending except the excise/estate edge cases noted below. states-hub.html (added a Puerto Rico row to the "Live now" table), master-extension-matrix.html (build-status table), index.html, llms.txt, search-index.json (76 entries), and sitemap.xml were all updated in the same batch.

**Corrected in-session (T5, caught during verification, never shipped wrong):** New Hampshire's Interest & Dividends Tax was repealed effective for tax periods beginning 1-1-2025 (2024 was the last taxable year). `new-hampshire-extension.html` was rewritten to state this; only the Business Profits Tax (Form BT-EXT, N.H. Code Admin. R. Rev 307.09) still has an active extension mechanism there.

**Remaining gap (intentional, not an oversight):** excise tax and estate/inheritance edge cases have not been scoped or built. Every state, DC, Puerto Rico, and the 8 city/local jurisdictions the user asked for by name are live - this is genuinely full US jurisdictional coverage for the core individual/business income-tax extension use case.

**T7 - this session's corrections and additions:**
- **North Dakota citation corrected.** A user-requested re-verification found that N.D. Cent. Code Sec. 57-38-34(6) - previously cited alone for the "automatic" federal-conformity rule - actually only text reads "the tax commissioner may grant a reasonable extension of time for filing a return when, in the judgment of the tax commissioner, good cause exists." The statute never uses the word "automatic"; that description comes from the ND Tax Commissioner's own published administrative practice applying that same discretionary authority (confirmed via tax.nd.gov/filing-extension). `north-dakota-extension.html` was rewritten to state this precisely: the statute is cited for the Commissioner's general extension authority, and the "automatic" conformity-to-federal-extension practice is attributed to the Tax Commissioner's guidance, not the statute's text.
- **"Verified [date]" removed from every page.** Per explicit instruction, the "Verified 9-2X-2026" badge/tagline text and the "Verified against primary source [date]." sentence in each page's source-row were stripped from all 76 pages that had them (69 via a site-wide script, 3 - faq.html, federal-hub.html, federal-special-situations.html - by hand for edge-case phrasing). This is a display choice only; the underlying verification discipline (checking every fact against primary source before publishing) is unchanged.
- **New `resources.html` page**, added to the nav bar on every page (inserted between "Penalties & Interest" and "FAQ") and to `build/gen_states.py`'s `cat_nav()` for all future pages. Lists official IRS resources (Get an Extension, Free File, Direct Pay, EFTPS, Taxpayer Advocate Service) and two IRS-authorized commercial e-file providers, TaxExtension.com and FileLater.com (both confirmed live and IRS-authorized on their own sites this session), clearly labeled as non-endorsed, no affiliate relationship, official-first. Also fixed a pre-existing plain-text typo on `efile-and-third-party-services.html` ("taxextensions.com" with an s, unlinked) to correctly name and link both real providers and point to the new resources page.

**Quality gate**: all 77 HTML files pass lint (no em-dashes, no forbidden strings 109025/AC58472/P00646638/93250/Knyazev/"Prepared by", disclaimer link present, canonical tag present, balanced divs, Map and Resources nav links present, no leftover "Verified" text). extensions-data.json (75 records), search-index.json (77 records) validated as JSON; sitemap.xml (77 entries) validated as XML - all three counts cross-checked against the 77 files on disk with zero mismatches and zero orphan/dangling links. All 125 external URLs site-wide curl-verified this session; the 11 that return 403/302/000 to curl (github.com, mtc.dor.state.ma.us, mypath.pa.gov, otr.cfo.dc.gov, tax.ri.gov, kcmo.gov, mass.gov x2, ny.gov, revenue.nh.gov, taxes.marylandtaxes.gov) were each independently re-confirmed real and on-topic via WebFetch or WebSearch this session - bot-blocked/egress-blocked, not broken. No genuine 404s remain unfixed as of this handoff.

## BLOCKER: git push is not working

Every `git push origin main` attempt fails identically:

```
remote: access denied by the git proxy: tk-cpa/extensionguide is not in this
session's authorized repository set, so the proxy will not inject a credential
for it. To fix, add the repository to the session's sources.
fatal: unable to access 'https://github.com/tk-cpa/extensionguide.git/':
The requested URL returned error: 403
```

This is a session-level/connector-level allowlist issue on Anthropic's proxy, not a GitHub permissions problem. It has persisted across every session so far. The established workaround, used every batch: commit locally as normal (preserves history for whenever push starts working), then zip the site directory and deliver via SendUserFile for the user to upload manually through GitHub's web "Add file -> Upload files" UI in `tk-cpa/extensionguide`. Do not dwell on this blocker in conversation - attempt the push once per commit, note the result in one line if at all, and move straight to the zip/delivery step.

## DNS / HTTPS status

4 GitHub Pages A records at the registrar (GoDaddy) for `@`: 185.199.108.153, .109.153, .110.153, .111.153. `www CNAME -> extensionguide.com` left in place. DNS propagated; HTTPS/SSL provisioning status should be spot-checked periodically in Settings -> Pages -> "Enforce HTTPS" but needs no action absent a problem report.

## Repo structure and conventions (must follow for all future pages)

- Static HTML, no build step, all CSS/JS inline in each page (no external files except Google Fonts, D3/topojson for map.html).
- Design system: coral #F65F5A / ink #111111 / paper #FAFAF7. Oswald (display) + Inter (sans) + Courier New (mono) via Google Fonts.
- Canonical page anatomy: sticky nav (brand wordmark + tk.cpa CTA button) -> dark cat-nav bar (Home / Federal / States / Map / Master Matrix / Penalties & Interest / Resources / FAQ / Disclaimer) -> dark-hero `.ph` block (h1/sub/badges) -> `.article` content -> `.source-row` citation line -> print button -> `.disc` disclaimer line -> dark footer bar ("a tk.cpa resource" + tk.cpa link). Pages no longer show a "Verified [date]" badge or source-row sentence - that was removed site-wide per explicit instruction; don't re-add it to new pages. Caution: the historical one-off batch scripts under `build/` (pages_states3.py through pages_states5.py, pages_local1.py, page_pr.py) still have "Verified [date]" baked into their `sub` and `source_row` string literals from when they were written - they were run once to produce the live HTML and are not meant to be re-run verbatim. If a future session re-runs any of them, strip the "Verified ..." fragments from the regenerated output, or better, edit the affected page directly instead of regenerating it from the old script.
- Shared build library: `build/gen_states.py` (CSS constant, `cat_nav()`, `FOOTER`, `page()` function). Batch scripts (e.g. `build/pages_states5.py`, `build/pages_local1.py`) import from it and call `page()` once per jurisdiction, then are executed with `python3 build/<script>.py`.
- `extensions-data.json` is the single source of truth. Schema: jurisdiction, tax_type, filer, form, form_name, automatic, federal_extension_honored, payment_triggered_only, length, payment_threshold, efile_url, form_pdf_url, about_form_url, paper_address_source_url, authority, source_url, verified_as_of. master-extension-matrix.html renders it client-side via fetch, splitting on `jurisdiction === 'Federal'` vs everything else (state and local jurisdictions share the same non-federal table).
- Every green-box "Get the form" callout links directly to the official PDF plus the tax authority's About/instructions page - never a third-party form mirror.
- Content hard rules (per tk.cpa operating framework): no em dashes; forbidden strings 109025/AC58472/P00646638/93250/Knyazev/"Prepared by" must never appear; capitalize Client/Clients (n/a on this public site); full 4-digit years; zero extra branding beyond "a tk.cpa resource" footer pattern already established.
- SEO/AI files to update on every batch: sitemap.xml, llms.txt (llmstxt.org format), search-index.json, and master-extension-matrix.html's build-status table.
- Before any commit: run the lint gate (forbidden strings, em-dash, disclaimer link presence, canonical tag presence, div-tag balance, Map nav link) and curl-verify every external link returns 200 (curl 403/000 on a known-government/legal host is not automatically a break - confirm via WebFetch/WebSearch before concluding it's fine, and always chase down and fix an actual 404).
- Filenames: `<state-name>-extension.html` lowercase with dashes for states; `<city-name>-extension.html` for local jurisdictions (consolidate a whole state's municipalities into one page when a single statewide statute governs them, as with Ohio and Michigan); `federal-<form>-<description>.html` for federal.

## T8 additions (this session)

- **estate-inheritance-tax-extensions.html** (NEW): one consolidated page covering state estate tax extensions for 13 states + DC (CT, HI, IL, ME, MD, MA, MN, NY, OR, RI, VT, WA, DC) and state inheritance tax extensions for 4 states (KY, NE, NJ, PA). Maryland has both taxes and appears in both tables. Gift tax extensions are scoped as federal-only (Form 4768/IRC sect 6081) - Connecticut is the only state with its own gift tax and its extension rides the same combined CT-706/709 EXT form as its estate tax.
- **extensions-data.json**: grew from 75 to 92 records (17 new: 13 estate-tax states + DC, 4 inheritance-tax-only states).
- **Two open items flagged, not fabricated**: Kentucky (KRS ch. 140) - no general filing-extension provision was located in the statute; only a deferred-payment election for certain future interests exists (KRS 140.210/140.220-140.224), which is not the same thing. Nebraska - inheritance tax is filed with the county court as part of probate, not via a Department of Revenue extension form; there is no formal filing extension, only a county-court penalty-abatement mechanism for good cause. Both are stated as open items on the page itself, not rounded up to "verified."
- **Statute citations pinned to exact sections via primary source this session** (not carried over from memory): Conn. Gen. Stat. sect 12-392; Haw. Rev. Stat. sect 236E-9; 35 ILCS 405/6; 36 M.R.S. sect 4110; Md. Code Tax-Gen. sect 7-305.1; Minn. Stat. sect 289A.19 subd. 4; N.Y. Tax Law sect 976(a)(1); Or. Rev. Stat. sect 118.100; 32 V.S.A. sect 7481; RCW 83.100.050; D.C. Code sect 47-3705(b); Neb. Rev. Stat. sect 77-2010. Massachusetts, Rhode Island, New Jersey, Illinois, and Pennsylvania are cited to the governing chapter/regulation plus the agency form/instructions rather than one pinpoint subsection, because a live primary source naming the exact subsection was not found this session - this is disclosed, not glossed over.
- Cross-linked from index.html, states-hub.html, and master-extension-matrix.html (build-status table, note-box, and a new stateTable row set per jurisdiction that renders automatically from extensions-data.json). Not added to the persistent top cat-nav, consistent with how every other individual content page (state pages, local-jurisdiction pages, federal form pages) is linked from hub pages rather than the top nav.
- sitemap.xml, search-index.json, llms.txt all updated. File count: 78 HTML pages (up from 77).

## T9 additions (this session)

- **excise-tax-extension.html** (NEW): verified finding that Form 720 (quarterly federal excise tax) and Form 2290 (heavy highway vehicle use tax) have no extension of time to file - neither is on the Form 7004 eligible-forms list, confirmed against each form's current IRS instructions. State excise tax is explicitly scoped out as too fragmented for a single page, with a cross-link to Washington's B&O excise page which already covers that state's mechanism.
- **sales-and-use-tax-extension.html** (NEW, then expanded three times mid-session to full coverage): all 45 sales-tax-imposing states plus DC (46 jurisdictions) checked against primary sources this session. 33 report no general sales tax filing extension (sales tax is a trust-fund tax, structurally different from income tax); 13 confirmed exceptions, each verified against its own primary source: Washington, Virginia, Wisconsin, Louisiana, New Mexico, Kansas, Oklahoma, West Virginia, Hawaii, Maine, Mississippi, Wyoming, Arkansas. Two of the thirteen (Oklahoma, Arkansas) rest on general "any state tax law" statutory language rather than a sales-tax-specific provision, and the page says so explicitly rather than treating general authority as sales-tax-specific guidance. Alaska, Delaware, Montana, New Hampshire, and Oregon have no general state sales tax and are out of scope by definition, not by omission.
- **other-extensions.html** (NEW): federal payroll tax (Forms 941, 940) has no extension mechanism (not on the Form 7004 list; only a 10-day filing accommodation for on-time depositors, which is not a request-based extension). FBAR (FinCEN Form 114) gets an automatic extension to October 15 with no form or request required - confirmed via FinCEN's own guidance.
- extensions-data.json grew from 92 to 111 records (2 excise, 2 payroll, 1 FBAR, 13 sales tax state-level records with a positive rule to encode; the other 33 states/DC checked for sales tax are documented in the page's table but not separately added as JSON records since they returned "no provision located").
- All three pages cross-linked from index.html, master-extension-matrix.html (build-status table + note-box), and each other's related-links block. sitemap.xml, search-index.json, llms.txt updated. File count: 81 HTML pages (up from 78).
- Full quality gate re-run clean: zero forbidden strings, zero em-dashes, zero "Verified [date]" text, zero div imbalance, zero orphan/dangling links, 81/81/81 disk/sitemap/search-index match.

## Next build queue

1. FAQ buildout is done - faq.html now has entries for estate/inheritance, excise, sales/use tax (full coverage), and payroll/SUI/FBAR.
2. Maintain a progress register tracking every jurisdiction x tax-type record's status (verified/drafted/live) - not yet built as a standalone file; could live as a project doc or a simple markdown table in the repo.
3. Illinois and Rhode Island estate tax citations are now pinned to exact subsection, closing the last unpinned items on that page: Illinois's extension rule lives in the underlying statute, not Part 2000 of the administrative code (which has no extension section) - 35 ILCS 405/8(c) ties the Illinois due date to whatever extended date the IRS grants on the federal return. Rhode Island's rule is likewise statutory, not in the income-tax-only extension regulation (280-RICR-20-55-2, which was a false lead) - R.I. Gen. Laws sect 44-23-3 authorizes an extension "corresponding to the approved extension granted by the Internal Revenue Service for the filing of federal form 706." Both found by searching the enabling statute chapter directly (35 ILCS 405 sections list; R.I. Gen. Laws ch. 44-23 index) rather than the administrative code once the administrative code proved not to be where the provision lives.
4. State unemployment insurance (SUI/SUTA) survey is now complete: all 50 states plus DC are addressed on other-extensions.html, and all 51 jurisdictions are resolved to a definitive, cited answer - zero open items remain on this survey. The key move that closed most of the earlier "not yet verified" states was switching from agency web pages (which often don't discuss extensions at all) to the underlying statute or administrative regulation, which frequently does. Six confirmed good-cause exceptions: Arkansas (up to 30 days, Regulation No. 6 Section F), Idaho (up to 60 days), Missouri (up to 3 months, 8 CSR 10-4.030), New Hampshire (30 days on timely application plus a $50 fee, RSA 282-A:142), New Mexico (up to 30 days, 11.3.400.410 NMAC), and Washington (good cause, with a deposit requirement, WAC 192-310-030(11)) - all six now extensions-data.json records (117 total). Three narrow, non-general exceptions: Connecticut (15 extra days, reimbursing employers only), Tennessee (a payment extension only, not filing, Tenn. Comp. R. & Regs. 0800-10-01-.05), and South Carolina (also payment only). The remaining states with a definitive answer show no general extension mechanism (Michigan's only accommodation was a 2026 one-time technical waiver tied to a platform migration; North Carolina's was disaster-specific relief).

**New Mexico resolution (closed this round):** the dws.nm.gov domain rejected every direct fetch attempted across this session (4+ distinct URLs, all "requested URL was rejected" server-level rejections) - never resolved on that domain. It was closed by pivoting sourcing strategy: searched for the specific NMAC part governing employer contribution reports (11.3.400 NMAC, "Tax Rule"), found the section index and exact section number (11.3.400.410, "Extension of Due Date for Filing Quarterly Reports or Payment of Contributions") via the New Mexico State Records Center and Archives (srca.nm.gov - a different, non-blocking domain that hosts the same regulation as the state's official register), then independently cross-checked the operative text against two more independent secondary republications (regulations.justia.com and law.cornell.edu/regulations), all three agreeing verbatim on the "written application... good cause... not to exceed 30 days" language. This is the general pattern worth remembering for any future "source blocked" state: if an agency's own domain refuses automated retrieval, search for the specific NMAC/CSR/WAC-style regulation number first, then pull it from the state's own archives/register site or a law-library aggregator instead of the agency page - the citation is to the regulation, not to whichever website happens to host it.
5. Kentucky and Nebraska inheritance-tax items are now closed as confirmed findings, not open items: every operative section of KRS ch. 140 (140.160, 140.210, 140.220, 140.222-140.224, 140.230) and Neb. Rev. Stat. sect 77-2010 was checked directly this session, and neither authorizes a general filing extension - Kentucky has only a payment-deferral installment election for shares over $5,000, and Nebraska has only a discretionary penalty abatement, not a deadline extension. estate-inheritance-tax-extensions.html now has zero open items.

## T10 additions (this session)

- **New Mexico SUI/SUTA closed** on other-extensions.html: 11.3.400.410 NMAC ("Extension of Due Date for Filing Quarterly Reports or Payment of Contributions") confirmed - up to 30 days on written application showing good cause, sourced via srca.nm.gov after dws.nm.gov continued to reject every direct fetch attempt, cross-checked against regulations.justia.com and law.cornell.edu/regulations. The SUI/SUTA survey now has zero open items across all 50 states plus DC. extensions-data.json record added (118th at the time).
- **estate-inheritance-tax-extensions.html reaches zero open items**: Kentucky and Nebraska inheritance tax reconfirmed as genuine "no general filing extension exists" findings (every operative KRS ch. 140 section and Neb. Rev. Stat. sect 77-2010 checked directly), reframed from open-item language to confirmed findings. Illinois authority corrected from 35 ILCS 405/6 to the actual extension provision, 35 ILCS 405/8(c). Rhode Island authority pinned from the chapter-level ch. 44-23 citation to the pinpoint R.I. Gen. Laws sect 44-23-3. index.html, faq.html, master-extension-matrix.html, and llms.txt all had their "2 open items" / "open item" language removed and replaced with the zero-open-items finding.
- **Four new standalone local-jurisdiction pages added to build/pages_local1.py and wired site-wide** (search-index.json, llms.txt, states-hub.html, master-extension-matrix.html, sitemap.xml, extensions-data.json, and a cross-link/note-box on the related state page):
  - `pennsylvania-local-eit-extension.html` - Pennsylvania's Act 32 local Earned Income Tax (EIT), imposed statewide by ~2,500 municipalities/school districts but administered through a single Act 32 framework. Confirmed no extension provision exists anywhere in the Local Tax Enabling Act (53 P.S. sect 6924.502(c)(1)) or its regulation (12 Pa. Code Chapter 151) via a full-text search of the 66-page act (captax.com mirror, legis.state.pa.us blocked with a disguised IIS 403). Tax collectors (Berkheimer, Keystone) grant extensions only as administrative practice, not codified law.
  - `kentucky-local-occupational-tax-extension.html` - no statewide extension statute for Kentucky's roughly 200 independently-governed city/county/school-district occupational license tax jurisdictions (KRS 160.482-.488, KRS 92.281 checked directly, neither addresses extensions). Two county ordinances sampled (Jessamine County/Nicholasville, Logan County) show a common federal-extension-matching pattern - explicitly disclosed on the page as a 2-jurisdiction sample, not an exhaustive survey of ~200 jurisdictions.
  - `denver-opt-extension.html` - Denver's Occupational Privilege Tax (OPT), DRMC sect 53-237 to 53-244. No extension mechanism found after a direct text search of over 60,000 characters across 4 primary Denver documents (general tax booklet, Tax Guide Topics 61/30/68, the annual OPT return itself), all downloaded via curl since denvergov.org blocked WebFetch with robots.txt errors.
  - `wilmington-de-extension.html` - City of Wilmington Net Profits Tax (Form WCWT-6) confirmed to honor a federal IRS extension - a copy must be filed with the Earned Income Tax Division by the original due date; payment not extended (one-time 5% penalty plus 1.5%/month interest on unpaid balances). City of Wilmington Earned Income Tax Regulations sect 220 and sect 601a, sourced from the current WCWT-6 instructions (zillionforms.com mirror, legis-hosted PDF blocked by JS-rendered Municode library).
- **maryland-county-local-tax-extension.html (NEW)**: Maryland's 23 counties and Baltimore City confirmed to have no separate local income tax return or local extension - local tax is a line item on the same Form 502 as state tax, covered by the same automatic 6-month Form 502E extension. Md. Code Ann., Tax-Gen. sect 10-823; Comptroller Administrative Release No. 4. Same structural finding as the existing Indiana county-tax page.
- **indiana-extension.html**: added a note-box confirming Indiana county income tax is computed on Schedule CT-40/CT-40PNR attached to the same IT-40 return, not separately filed, so the state's existing extension automatically covers it (Thomson Reuters UltraTax CS guidance, Indiana DOR Bulletin #28).
- extensions-data.json grew from 117 to 122 records this session (New Mexico SUI/SUTA, Pennsylvania local EIT, Kentucky local occupational tax, Denver OPT, Wilmington DE Net Profits Tax, Maryland county local tax).
- File count: 86 HTML pages (up from 81 at the last handoff sync). Full quality gate (forbidden strings, em-dash, div balance, JSON/XML validity, sitemap/search-index/disk cross-check, dangling-href check) re-run and passes clean after every batch this session.
- Per explicit, repeated user instruction this session: no incremental zip/SendUserFile deliveries were made, and no interim check-in messages were sent between batches - all progress was committed to git silently. The git-push-403 blocker (see above) persists unchanged; every commit this session remains local/unpushed.

## T11 additions (this session)

- **federal-5558-employee-plan-extension.html (NEW)**: Form 5558 (Application for Extension of Time to File Certain Employee Plan Returns) - closed a genuine prior gap in federal coverage. One-time automatic extension to the 15th day of the 3rd month after the normal due date for Form 5500, Form 5500-SF, Form 5500-EZ, and Form 8955-SSA, if filed by the normal due date. IRC sect 6081. Documents the exception where the plan return automatically rides the employer's own federal income tax extension when plan year and tax year match, and that this does not extend the PBGC Form 1 Annual Premium Payment. Sourced directly from the current Form 5558 (Rev. January 2025) and its instructions.
- **colorado-other-cities-opt-extension.html (NEW)**: covers the four Colorado Occupational Privilege Tax (OPT) cities besides Denver - Aurora, Greenwood Village, Sheridan, and Glendale. Each checked directly against a primary source this session (return form or, for Greenwood Village, the full taxpayer guide) - no extension mechanism found for any of the four, matching the already-documented Denver finding. All five Colorado OPT cities are now resolved.
- Both pages cross-linked from their respective hub/related pages (federal-hub.html, index.html, colorado-extension.html, denver-opt-extension.html) and wired into search-index.json, llms.txt, sitemap.xml, and extensions-data.json.
- extensions-data.json grew from 122 to 127 records this session (Form 5558, Aurora, Greenwood Village, Sheridan, Glendale).
- File count: 88 HTML pages. Full quality gate re-run clean after every batch - one unclosed div tag was caught and fixed in colorado-other-cities-opt-extension.html before commit.
- Per explicit, repeated user instruction: no incremental deliveries and no interim check-in messages this session - all progress committed to git silently, delivered as a summary only when the user asks directly ("100% done? 10/10?").
- **Genuine scope ceiling, stated plainly when asked**: "every single extension of any kind in USA" read literally means every one of roughly 19,000 US municipalities and thousands of counties/school districts that impose a local tax - not verifiable to 100% certainty in any single session. This is a structural ceiling, not an oversight. Current local-jurisdiction coverage (17 named jurisdictions/groups as of this handoff) is genuine and verified, not exhaustive of the full municipal universe.

## T12 additions (this session)

- **international-information-returns-extension.html (NEW)**: a significant prior gap closed - how the extension rules work for Forms 3520, 3520-A, 5471, 5472, 8865, and 8938. Confirmed directly against each form's current IRS instructions: Forms 3520, 5471, 8865, and 8938 ride on the filer's own income tax return extension with no separate form; Form 5472 rides on the reporting corporation's own Form 7004 (or, for a foreign-owned disregarded entity, a Form 7004 filed under the DE's own EIN attached to a pro forma Form 1120). Form 3520-A is flagged as the key exception: the foreign trust must file its own Form 7004 under its own EIN by the 15th day of the 3rd month after its tax year end - the U.S. owner's own Form 4868 or Form 7004 does NOT extend it, quoted directly from the form's own instructions ("An extension of time to file an income tax return does NOT extend the time to file Form 3520-A").
- extensions-data.json grew from 127 to 133 records this session (6 new: Forms 3520, 3520-A, 5471, 5472, 8865, 8938).
- Wired into federal-hub.html, index.html, search-index.json, llms.txt, sitemap.xml.
- File count: 89 HTML pages. Full quality gate re-run clean.

## T13 additions (this session)

- **international-information-returns-extension.html expanded**: added Form 8621 (PFIC shareholder reporting, including the QEF election under section 1295) and Form 8854 (Initial and Annual Expatriation Statement for covered expatriates), both confirmed against current IRS instructions to ride on the filer's own income tax return extension. The page now covers 8 forms: 3520, 3520-A, 5471, 5472, 8621, 8854, 8865, 8938.
- extensions-data.json grew from 133 to 135 records.
- File count: 89 HTML pages (no new page this batch, existing page expanded). Full quality gate re-run clean.

## Delivery workflow until push is fixed

1. Build the batch (new HTML pages + updated extensions-data.json + updated nav/map/sitemap/llms.txt/search-index.json/master-matrix/states-hub/index).
2. Run the lint gate script, JSON/XML validation, and the link-verification curl loop over every external URL site-wide (not just the new batch - a shared file like map.html or states-hub.html can pick up a stale link during editing).
3. `git add -A && git commit -m "..."` locally (keep committing normally even though push is blocked - preserves history for whenever push starts working).
4. Attempt `git push origin main` once; expect the identical 403 above; do not dwell on it.
5. `zip -r /mnt/user-data/outputs/extensionguide-<batch-name>.zip . -x ".git/*" -x "build/__pycache__/*"` from `/home/claude/extensionguide`.
6. SendUserFile the zip.
7. Ask the user to upload via GitHub's web "Add file -> Upload files" UI (uploading the full file set overwrites in place; GitHub Pages rebuilds automatically from `main`).

## T14 additions (this session)

- **us-territories-extension.html (NEW)**: US Virgin Islands, CNMI, Guam, and American Samoa. USVI confirmed - bona fide residents file a paper Form 4868 with the VI Bureau of Internal Revenue (not IRS); nonresidents file separately with both IRS and VI BIR, and VI BIR honors an extension timely filed with IRS (IRS Publication 570). CNMI confirmed dual-track - mirror-code income tax follows federal Form 4868/7004 mechanics filed with CNMI Division of Revenue and Taxation; local Wage and Salary Tax/Earnings Tax has its own extension per 4 CMC sect 1815(a) (length not fixed in statute); local Gross Revenue Tax has NO extension to file at all, 4 CMC sect 1815(b) - only a reasonable-cause penalty waiver under 4 CMC sect 18134. Guam and American Samoa explicitly disclosed as not independently confirmed this session (Guam's extension info sits behind a MyGuamTax.com login wall; American Samoa's primary source site returned a server error) rather than assumed to follow the USVI/CNMI pattern.
- **state-pte-elective-tax-extension.html (NEW)**: partial survey of the 5 highest-volume state PTE elective tax (PTET/BAIT) states, each verified against a current primary source this session. California: no extension for the election or payment at all - Form FTB 3893 payments due "without regard to any extension of time for filing the return"; election sunsets after the 2025 taxable year. New York: 6-month extension to file the PTET annual return, requested online only through Business Online Services (no paper form) - filing extension only, not payment. New Jersey: separate Form PTE-200-T, 6 months, requires 80% of BAIT liability paid by the original due date or the extension is retroactively denied. Connecticut: separate Form CT-PET EXT, 6 months, must be filed/paid electronically, a federal Form 7004 extension does not automatically extend the Connecticut filing date. Illinois: no separate mechanism - PTE tax rides on the entity's own Form IL-1065/IL-1120-ST, which gets Illinois' automatic 7-month filing extension, but payment is still due on the original date regardless. Explicitly disclosed as a partial survey - over 30 states have enacted a PTET and are not all covered.
- **iowa-school-district-surtax-extension.html (NEW)**: Iowa school district and EMS surtax confirmed to have no separate return or extension - computed as a percentage of state tax on IA 1040 line 19, added into "Total state tax and local surtax" on line 20, and covered by Iowa's own existing automatic 6-month extension (90% payment test, Iowa Code sect 422.21). Same piggyback pattern as the existing Maryland county and Indiana county-tax findings.
- **oregon-transit-self-employment-tax-extension.html (NEW)**: TriMet and Lane Transit District self-employment tax (Form OR-TM) confirmed to have no separate extension form - a federal extension or an Oregon individual income tax return extension automatically extends Form OR-TM (check a box on the return); payment is still due by the original due date regardless ("Oregon doesn't allow an extension of time to pay even if the IRS allows an extension," 2025 Form OR-TM Instructions).
- **alabama-municipal-occupational-tax-extension.html (NEW)**: Alabama has no statewide statute for municipal occupational tax extensions - each of roughly 15-20 cities sets its own rule by ordinance. Birmingham independently confirmed this session (Occupational Tax Code, Ordinance No. 97-184, Section 6): the Director of Finance may grant a discretionary extension for reasonable cause, capped at 30 days from the original due date, with interest and penalty continuing to accrue during the extension. Other Alabama municipalities (Bessemer, Gadsden, etc.) explicitly disclosed as not independently checked this session - same disclosed-sample approach as the existing Kentucky local occupational tax page.
- All 5 new pages wired into states-hub.html (states hub + local jurisdictions table), master-extension-matrix.html, search-index.json, llms.txt, sitemap.xml, extensions-data.json, and cross-linked from their most relevant existing pages (state pages for CA/NY/NJ/CT/IL/IA/OR/AL, federal-hub.html not touched this batch, Kentucky/Maryland/Indiana/Denver pages referenced in related-links).
- extensions-data.json grew from 139 to 147 records this session (4 territory records, 5 PTET records, 3 local records - Iowa, Oregon, Alabama Birmingham).
- File count: 94 HTML pages (up from 89 at the T13 handoff sync). Full quality gate (forbidden strings, em-dash, div balance, JSON/XML validity, sitemap/search-index/disk three-way cross-check, dangling-relative-href check) re-run clean after both commits this batch.
- Two commits this batch: `ea2ac60` (US territories, made just before this handoff sync period started) is already covered in the commit log; the PTET commit and the Iowa/Oregon/Alabama commit are both new. Git push remains blocked by the same session-scoped proxy 403 (repository not in this session's authorized set) - the user has confirmed they upload to GitHub themselves, so this is not being treated as an open item requiring further action, and per explicit standing instruction it is not being raised again unless the user asks directly.
- This batch completed items 1, 2, and 3 of the 5-item roadmap given when the user asked "if we were to continue and make it more complete what would we do," per the user's follow-up "do you think you can do 1, 2, 3 in the next batch possibly?" Items 4 and 5 of that original roadmap (not yet started, no commitment made to attempt them without further instruction) were not specified in detail in this handoff - see the prior planning answer in the live conversation for their content if picking this back up.

## T15 - full-site review and handoff sync (this session)

**Context**: local git history in this container's session is an orphaned, unrelated history from what is actually deployed - the live site is maintained by the user re-uploading files via GitHub's web "Add file -> Upload files" UI (or from a different, authorized session/environment), not by this session's `git push` (still blocked - see below). Before this review, this session reset its local working tree to match `origin/main` exactly (`git fetch` + `git reset --hard origin/main`) so the review runs against what is actually live, not a stale local copy.

**Review performed:**
1. Quality gate re-run against the live tree: forbidden strings, em-dash check, div-tag balance, JSON validity (extensions-data.json: 147 records, search-index.json), XML validity (sitemap.xml), and the three-way disk/sitemap/search-index URL cross-check - all exact, zero mismatches, zero errors. 94 HTML pages confirmed live.
2. Dangling-relative-href sweep across all 94 pages - zero broken internal links.
3. External-link sweep - extracted all 132 distinct external (non-extensionguide.com) URLs referenced site-wide and checked each. curl flagged 8 as non-2xx (all 403). Re-checked each of the 8 via a second, independent fetch path (WebFetch tool, different network egress than this session's curl/proxy path): 7 of the 8 (github.com/topojson/us-atlas, otr.cfo.dc.gov, mass.gov x2, tax.ri.gov, revenue.nh.gov, keystonecollects.com) loaded genuine live content on the second check - confirming the curl 403s were this session's own proxy/WAF fingerprint being blocked, not actually dead links. The 8th, kcmo.gov/city-hall/departments/finance/tax-home/tax-forms, returned 403 on both check methods and could not be independently confirmed either live or dead this session - disclosed as an open item, not assumed either way.
4. Found and fixed one genuine regression: `alabama-extension.html` had lost its cross-link to `alabama-municipal-occupational-tax-extension.html` (the related-links line was missing). Root cause: a same-day GitHub web upload ("test: cowork push access check" / its follow-up removal commit, both from a `tk-cpa-cowman` identity, made from outside this session) briefly overwrote `alabama-extension.html` with a copy that predated that cross-link, along with a one-line CNAME formatting change (trailing newline dropped - cosmetic, not fixed, does not affect functionality). Fixed and committed locally as `370a949`.

**Push status - resolved this session, but via a different path than the cloud container's own git proxy.** Pushing `git push origin main` directly from this session's cloud container is still blocked by that container's own outbound git proxy at the infrastructure level (repository-authorization based, independent of token validity):

```
remote: access denied by the git proxy: tk-cpa/extensionguide is not in this session's authorized repository set, so the proxy will not inject a credential for it. To fix, add the repository to the session's sources.
```

That block was NOT fixed by a new GitHub personal access token - a new token does not change which repos the cloud container's proxy authorizes. What actually worked: using the `mcp__remote-devices__device_bash` tool (a separate Linux VM on the user's own linked Windows machine, reached through the desktop app bridge, with its own independent network path - not behind the cloud container's proxy at all) to install the GitHub CLI, authenticate with a user-supplied classic PAT (scopes: repo, read:org), clone the repo fresh into that VM's own $HOME (never inside a mounted/connected folder - git's file locking breaks there), and push from there. That push succeeded (`e6e9833..1d98022 main -> main`) and applied the one real fix found this session (see below).

**Do not write the actual token value into this file or any other git-tracked file in this repo - it is public.** Ask the user directly for a token when one is needed; store it only in memory for the duration of the shell commands, never in a committed file, and never in claude.ai Project docs either unless the user explicitly says that doc is meant to hold it.

**A new session picking this project up**: if `git push` from the cloud container's own `Bash` tool fails with the proxy-authorization message above, do not keep retrying with different tokens - that error is not about the token. Instead check whether `mcp__remote-devices__device_bash` tools are available (a linked desktop) and use the device_bash + gh CLI path documented above, or fall back to the zip-and-upload delivery path.

**File count and data at this handoff: 94 HTML pages, 147 extensions-data.json records** (matching what's actually live on origin/main as of this sync, not a locally-inflated count).

**Recommended posture for a new session picking this up**: trust `origin/main` as ground truth (this session's local git history is disconnected from it - always `git fetch && git reset --hard origin/main` before starting new work, not `git pull` or a merge, since the histories don't share an ancestor). Re-run the same three checks above (quality gate, internal links, external links) as a first step before adding new content, since files can silently regress via manual GitHub uploads between sessions, as just happened once already.

## T16-T17 additions (this session)

**Push mechanism update**: `mcp__remote-devices__device_bash` required at least one connected folder to unlock at all this session (a `device_request_folder_access` call was needed before `device_bash` would run, even though the actual git work happens outside the mounted folder in the device VM's own `$HOME`, unchanged from the T15 pattern). The user granted `Documents`. gh CLI is not pre-installed on the device VM and does not persist between sessions - it was downloaded fresh from `github.com/cli/cli/releases` (a tarball, not apt) and extracted into `$HOME` each time. The user's GitHub PAT was requested fresh in chat both times (not persisted anywhere, per standing instruction), used only as `GH_TOKEN` for `gh`/`git`, and the gh install plus the cloned repo were both deleted from the device at the end of each batch. This full cycle (request folder access -> install gh -> ask for token -> clone into `$HOME` -> copy changed files in via `device_commit_files` staged through the connected folder -> commit -> push -> clean up) is the current working delivery path and should be repeated by a future session rather than reinvented - it pushed cleanly both times this session (`1f1f648`, then a second commit for T17 cleanup).

**T16 - Guam and American Samoa verified**: `us-territories-extension.html` updated. Guam confirmed as a full mirror-code territory (48 U.S.C. sect 1421i(a)-(b)) - the Form 4868 mechanism applies, filed with Guam's Department of Revenue and Taxation instead of the IRS, labeled a reasonable inference from the statute (no Guam-issued bulletin naming Form 4868 directly was found). American Samoa confirmed NOT mirror-code (own local code frozen to the IRC as of December 31, 2010, ASCA Title 11) and confirmed to require two separate returns for bona fide residents - a territorial return with the ASG Treasury Tax Office and a US return excluding American-Samoa-source income with the IRS (IRS Publication 570). The US return's Form 4868 extension is confirmed; the territorial return's own extension mechanism remains a genuine open item - no ASCA provision or ASG Treasury guidance establishing one was located after a direct search this session. extensions-data.json grew from 147 to 148 records (Guam only - no record was added for American Samoa's unconfirmed piece, consistent with the site's practice of not adding a record for "no provision located").

**T17 - site-wide "Verified [date]" sweep**: the T7 stripping of the "Verified [date]" badge/tagline and "Verified against primary source [date]." sentence (a standing display-only rule - the underlying verification discipline is unchanged) only covered pages live at the time. 18 pages built in later sessions (T10 through T14 - the local-jurisdiction pages, PTET page, Iowa/Oregon/Alabama transit and surtax pages) never had it stripped and were caught during this session's own quality-gate pass. Removed via a site-wide regex sweep (the `&nbsp;&bull;&nbsp; Verified M-D-YYYY` hero-subline fragment and the "Verified against primary source [date]." source-row sentence), verified against every remaining "verified" occurrence site-wide by hand to confirm none were the banned pattern (ordinary uses like "independently verified this session" or "verified against the 2025 Form OR-TM Instructions" are fine and were left alone). Full gate re-run clean: forbidden strings, em-dash, div balance, JSON/XML validity, 94/94/94 disk-sitemap-search-index cross-check, zero dangling relative hrefs, zero residual Verified-date text anywhere on the site.

**kcmo.gov link resolved**: the one link flagged in T15 as unconfirmed either way (403 on both curl and WebFetch) is now confirmed live via a third, independent method - Google's own search index returns the exact URL and page title ("City Tax Forms | CITY OF KANSAS CITY | OFFICIAL WEBSITE") alongside matching RD-111/RD-112 form references, meaning the site is indexing and serving that page even though it blocks automated fetchers from both this session's proxy paths. `kansas-city-extension.html`'s source-row now documents this. No genuine open items remain from the T15 external-link sweep.

**File count and data at this sync: 94 HTML pages, 148 extensions-data.json records.**

## T19 additions (this session)

**PTET page expanded from 5 to 13 states**: `state-pte-elective-tax-extension.html` grew to cover Massachusetts, Wisconsin, Minnesota, Colorado, Maryland, Georgia, Arizona, and Ohio alongside the existing California, New York, New Jersey, Connecticut, and Illinois. Pattern found: most of the new states (Illinois, Wisconsin, Minnesota, Colorado, Georgia, Arizona, Ohio) let the PTET ride on the entity's own regular return extension with no separate PTET-specific form; Massachusetts and Maryland require their own separate PTET extension application (Form M-63D-EXT and Form 510/511E respectively) layered on top of the entity's regular extension - matching the pattern already documented for New York, New Jersey, and Connecticut. California remains the outlier with no extension mechanism at all for the election or payment.

Every citation was checked this session rather than carried from training-data recall - a first draft included several statute section numbers (Minnesota, Colorado, Maryland, Arizona, Ohio) recalled from memory rather than verified, caught before publication and corrected: Colorado (C.R.S. sect 39-22-343), Arizona (A.R.S. sect 43-1014), Ohio (Ohio Rev. Code sect 5747.38), and Maryland (Md. Code Ann., Tax-Gen. sect 10-102.1) were each independently confirmed via a second source (Justia/FindLaw/state code sites showing the exact section number and matching subject matter). Minnesota's citation was corrected after a direct fetch of the actual statute text: Minn. Stat. sect 289A.19, subd. 1 governs the automatic 6-month extension mechanics (not sect 289A.08, subd. 7a alone, which only covers the PTE election itself - both are now cited together, each confirmed against the live statute text at revisor.mn.gov). Massachusetts's M.G.L. c. 63D citation was confirmed against malegislature.gov directly.

extensions-data.json grew from 148 to 156 records (8 new PTET records). Updated llms.txt, search-index.json, states-hub.html, and master-extension-matrix.html to reflect 13 states covered instead of 5. Full site quality gate re-run clean: forbidden strings, em-dash, div balance (19/19 on the PTET page itself, 14 table rows), JSON/XML validity, 94/94/94 disk-sitemap-search-index cross-check, zero dangling relative hrefs, zero residual Verified-date text.

Still not covered on the PTET page: Alabama, Arkansas, Idaho, Kansas, Louisiana, Michigan, Mississippi, New Mexico, North Carolina, Oklahoma, Oregon, Rhode Island, South Carolina, Utah, Virginia, among others - over 30 states have enacted a PTET and this remains a disclosed partial survey.

**File count and data at this sync: 94 HTML pages, 156 extensions-data.json records.**

## T20 additions (this session)

**PTET page expanded from 13 to 21 states**: `state-pte-elective-tax-extension.html` grew to cover Virginia, South Carolina, Michigan, North Carolina, Oklahoma, Louisiana, Rhode Island, and Utah alongside the existing 13. Pattern found: Virginia, South Carolina, Michigan, North Carolina, Louisiana, Rhode Island, and Utah all let the PTET ride on the entity's own regular return extension with no separate PTET-specific extension form; Oklahoma requires its own separate extension application (Form 504-PTE, in place of the entity's usual Form 504-C).

Two states are disclosed as a reasonable inference rather than a direct-quote confirmation: Louisiana's general partnership extension (Form IT-565's automatic 6-month extension) was confirmed to apply to partnerships generally, but no source found explicitly carved out or confirmed PTE electors under La. R.S. 47:287.732.2 by name - the entity's general extension instructions simply do not distinguish PTE electors from other partnerships. Rhode Island's Form RI-7004 (the entity's general business extension) was confirmed as the mechanism covering RI-1065/RI-1120S/RI-1120C, but no source directly named Schedule PTE (the PTE elective tax schedule) as covered by it specifically - reasonable inference from the fact that Schedule PTE is filed as an attachment to those same returns, not a standalone return. Both are labeled as such in the page and the JSON record.

Every citation was checked this session rather than carried from training-data recall. A first draft again included several statute section numbers recalled from memory rather than verified (Virginia, South Carolina, Michigan, North Carolina, Oklahoma, Rhode Island, Utah) - caught and corrected before publication, consistent with the same error made and caught in T19. Virginia (Va. Code Ann. sect 58.1-390.3), South Carolina (S.C. Code Ann. sect 12-6-545), Oklahoma (Okla. Stat. tit. 68, sect 2355.1P-4), and Rhode Island (R.I. Gen. Laws sect 44-11-2.3) were each confirmed as exact matches via independent legal-database search (Justia/FindLaw/law.lis.virginia.gov) showing the section number with matching subject-line title. North Carolina's first-draft citation (105-153.7) was wrong and has no such section; corrected to the two real sections after direct verification - N.C. Gen. Stat. sect 105-131.1A (S corporation "taxed pass-through entity" election) and sect 105-154.1 (the parallel partnership election), both confirmed via Justia and the NC General Assembly's own enacted-statute PDF. Utah's first-draft citation (bare sect 59-10-1403.2) was confirmed correct but imprecise; a direct fetch of the enacting bill (H.B. 444, 2022 Gen. Sess.) turned up a quote citing "the taxes imposed under Subsection 59-10-1403.2(2)" specifically, so the citation was tightened to the subsection. Michigan's first-draft citation (MCL 206.837) was wrong - a direct fetch showed that section covers an unrelated 2021 refund-interest exception - and no correct MCL section number for the Flow-Through Entity Tax Act's imposition provision could be independently confirmed this session, so the citation was pulled back to the Public Act number only (Mich. Pub. Act 135 of 2021), without a fabricated section number, consistent with the standing rule to disclose rather than guess.

extensions-data.json grew from 156 to 164 records (8 new PTET records). Updated llms.txt, search-index.json, states-hub.html, and master-extension-matrix.html to reflect 21 states covered instead of 13. Full site quality gate re-run clean: forbidden strings, em-dash, div balance (19/19 on the PTET page itself, 22 table rows = 1 header + 21 data rows), JSON/XML validity, 94/94/94 disk-sitemap-search-index cross-check, zero dangling relative hrefs, zero residual Verified-date text.

Still not covered on the PTET page: Alabama, Arkansas, Idaho, Kansas, Mississippi, and New Mexico, among others - over 30 states have enacted a PTET and this remains a disclosed partial survey.

**File count and data at this sync: 94 HTML pages, 164 extensions-data.json records.**

## T21 additions (this session)

**PTET page expanded from 21 to 27 states**: `state-pte-elective-tax-extension.html` grew to cover Alabama, Arkansas, Idaho, Kansas, Mississippi, and New Mexico - the last six states from the T19/T20 "not yet covered" disclosure list. Pattern found: Alabama, Idaho, Kansas, and New Mexico let the PTET ride on the entity's own regular return extension (Alabama and Kansas specifically via a copy of federal Form 7004 attached to the entity's own return; Idaho via the state's general automatic extension, no PTE-specific form; New Mexico via the federal extension or, absent one, Form RPD-41096); Arkansas and Mississippi require their own separate PTET/EPTE-specific extension application (Form AR1155PET and Form 83-180 respectively).

Two states are disclosed as a reasonable inference rather than direct-quote confirmation: Kansas' K-120S instruction booklet does not mention the SALT Parity Act at all, so the general Form 7004 extension is inferred to cover SALT Parity Act electors rather than confirmed by name. Mississippi's Form 83-180 extension guidance addresses pass-through entities generally without a direct quote naming EPTE electors specifically.

Every citation was checked this session, continuing the same discipline from T19 and T20. Alabama (Ala. Code sect 40-18-24.4, "Alabama Electing Pass-Through Entity Tax Act"), Idaho (Idaho Code sect 63-3026B, "Affected Business Entities - State and Local Taxation Treatment"), and New Mexico (N.M. Stat. Ann. sect 7-3A-10, "Election of entity-level tax; credit") were each confirmed as exact title/section matches via Justia. Arkansas' codified Act section could not be independently confirmed this session (search results kept surfacing the general Title 26 income tax chapter, not a specific PTE section), so the citation was pulled back to the confirmed administrative rule instead - 006.05.22 Ark. Code R. sect 003, "Arkansas Elective Pass-Through Entity Tax" (exact title match via Cornell LII and Justia) - rather than guess a codified Act section number. Kansas' SALT Parity Act has no single confirmable K.S.A. citation per the state's own Notice 22-16 ("New Sections 1-6 of the Bill create the SALT Parity Act"), so it is cited by its enacting bill, 2022 Kan. H.B. 2239, plus the notice itself. Mississippi's EPTE citation is likewise cited by its enacting bill, 2022 Miss. H.B. 1691, confirmed via the Mississippi Legislature's own bill-status pages, rather than a guessed Mississippi Code section number. No fabricated or unconfirmed statute section number was published this round.

extensions-data.json grew from 164 to 170 records (6 new PTET records). Updated llms.txt, search-index.json, states-hub.html, and master-extension-matrix.html to reflect 27 states covered instead of 21. Full site quality gate re-run clean: forbidden strings, em-dash, div balance (19/19 on the PTET page itself, 28 table rows = 1 header + 27 data rows), JSON/XML validity, 94/94/94 disk-sitemap-search-index cross-check, zero dangling relative hrefs, zero residual Verified-date text.

This closes out the PTET page's originally-disclosed "not yet covered" list from T19/T20 (Alabama, Arkansas, Idaho, Kansas, Mississippi, New Mexico) in full. The page remains an explicitly disclosed partial survey relative to the roughly 36-37 states that have enacted some form of PTET regime nationally - a handful of smaller/less-common PTET states (e.g., West Virginia, Delaware if applicable, and any newly enacted 2026 regimes) were not in scope for this research pass and are not claimed as covered.

**File count and data at this sync: 94 HTML pages, 170 extensions-data.json records.**
